#  SadTalker Microservice Requirements
# Production-ready dependencies for quality video generation

# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Redis for caching
redis==5.0.1
hiredis==2.2.3  # C extension for faster Redis operations

# SadTalker core dependencies (from existing requirements)
torch==1.12.1
torchvision==0.13.1
torchaudio==0.12.1
numpy==1.23.4
scipy==1.10.1
opencv-python==4.8.1.78
Pillow==10.0.1

# Audio processing
librosa==0.9.2
resampy==0.3.1
pydub==0.25.1
av==10.0.0

# Image processing and face analysis
face_alignment==1.3.5
imageio==2.19.3
imageio-ffmpeg==0.4.7
scikit-image==0.19.3
basicsr==1.4.2
facexlib==0.3.0
gfpgan==1.3.8
kornia==0.6.8

# Machine learning and math
joblib==1.1.0
tqdm==4.65.0
yacs==0.1.8
pyyaml
safetensors

# Performance and optimization
numba  # JIT compilation for faster processing
psutil  # System monitoring

# Logging and monitoring
structlog==23.2.0  # Structured logging

# Development and testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2  # For testing async endpoints

# Security
python-jose[cryptography]==3.3.0  # For JWT tokens if needed
passlib[bcrypt]==1.7.4  # For password hashing if needed

# CORS and middleware
# 'python-cors' does not exist on PyPI. Use 'starlette' or FastAPI's CORSMiddleware (built-in via Starlette).
# If you need CORS middleware, install 'starlette' (already pulled by FastAPI) or use 'fastapi' which provides CORSMiddleware.
# Removing invalid package reference.